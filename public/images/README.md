# Required photo assets

This landing page has 3 slots for real photography (see `lib/image-assets.ts`
for the full brief on each). No image-generation tool was available when
these were built, so each slot ships as a labeled placeholder until a real
photo is dropped in here under the exact filename below — no code changes
needed once you do.

- `hero-workspace.jpg` — Hero section visual
- `learner-reviewing-charts.jpg` — "Why pay for a course" section visual
- `journal-review-closeup.jpg` — "What You Get" section visual

See `lib/image-assets.ts` for the full content/style brief for each.
